#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<int> maxDepthAfterSplit(string seq) {
    vector<int> result;

    return result;
}

int main() {
    string seq{"(()())"};

    return 0;
}
